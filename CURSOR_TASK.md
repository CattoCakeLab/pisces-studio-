# Cursor 实施任务：双鱼工作室高保真滚动单页

## 先读这里

仓库已经完成 React + Vite + TypeScript 工程骨架、六段页面结构、内容类型、导航监听、减少动态效果检测、GSAP 清理接口和 Canvas 生命周期接口。

不要重新初始化项目、替换技术栈或删除既有公共契约。你的任务是基于现有骨架完成视觉、内容和动效。

## 目标体验与参考时间轴

参考视频总长约 51.5 秒，原始画面为 `944 × 906`。它展示的是自然平滑滚动的单页，而不是把视频直接作为背景。

1. **0–6 秒，Hero**：黑紫背景、中央柔和光晕、下落光线和模糊粒子；中文主标题居中，两侧有纵向装饰文字，桌面端使用鱼形自定义光标。
2. **7–11 秒，成员**：标题“双鱼游影”与两张人物卡片，进入视口时错峰显现。
3. **12–16 秒，内容**：标题“泛起涟漪”与三列文章卡片，卡片按顺序 reveal。
4. **17–29 秒，星图**：接近满屏的暗色 Canvas，四周漂浮星座节点；鼠标靠近时节点、连线和光尾产生弹性响应。
5. **30–35 秒，作品**：大型黑色视觉舞台，中央英文项目标题，随后进入中文项目标题、摘要和正文。
6. **36–42 秒，联系**：标题“投递信笺”、暗色玻璃表单、紫色描边按钮和极简页脚。
7. **43–51 秒，导航演示**：固定顶部导航在各区块间平滑跳转，并同步当前区块状态。

## 现有架构

- `src/app/App.tsx`：唯一页面入口，装配六个区块。
- `src/config/sections.ts`：唯一可信的 Section ID 和导航配置。
- `src/content/siteContent.ts`：所有可替换内容的集中入口。
- `src/types/content.ts`：内容和区块公共类型。
- `src/sections/`：六个区块；主要页面填充区域。
- `src/components/effects/`：星图和自定义光标的实现位置。
- `src/hooks/`：导航、减少动态效果、GSAP scope 和 Canvas RAF 生命周期。
- `src/styles/`：设计变量、全局规则和当前骨架样式。

## 不得破坏的契约

- 保留 `home`、`profiles`、`ripples`、`stars`、`work`、`contact` 六个 ID。
- 导航目标必须继续来自 `SECTION_DEFINITIONS`，不要复制另一份 ID 列表。
- 页面内容继续由 `siteContent` 注入，不要把最终文案写进 GSAP timeline 或 Canvas 算法。
- GSAP 组件使用 `useGsapScope()` 或等价的 `gsap.context()` 清理机制，React Strict Mode 下不得重复注册监听。
- 星图组件继续接受 `active`、`reducedMotion`、`className`；区块离屏或标签页隐藏时必须暂停 RAF。
- 表单不得调用真实接口，需明确提示当前为 Demo。
- 不得使用参考视频、视频截图或整页位图作为网页背景。

## 实施顺序

### 1. Hero

- 完成中央紫色光晕、粒子、光线、纵向装饰文案和进入提示。
- 在 `CustomCursor.tsx` 中实现内联 SVG 鱼形光标、弹簧跟随、朝向旋转和短光尾。
- 仅在 `pointer: fine` 且未开启 reduced motion 时启用自定义光标。

### 2. 成员与内容卡片

- 使用 `siteContent.profiles` 和 `siteContent.posts` 渲染，不改变数据流。
- 完成双列/三列桌面布局以及移动端单列布局。
- 使用 GSAP ScrollTrigger 做标题、卡片的淡入、上移和 stagger。

### 3. 互动星图

- 在 `ConstellationCanvas.tsx` 中接入 `useCanvasAnimation()` 的 `draw` 回调。
- 使用固定随机种子创建四组节点；按距离连线并让星座缓慢漂移。
- 鼠标进入影响半径时产生弹性牵引、亮度变化和短光尾，离开后回位。
- DPR 最高为 2；移动端减少节点与连线，触摸只触发点亮；reduced motion 只绘制静态帧。

### 4. 作品与联系

- 作品舞台保持大面积留黑，完成英文标题光晕、项目过渡和正文层级。
- 联系表单完成玻璃质感、焦点态、前端必填项/邮箱校验和本地提交提示。
- 添加简洁页脚，但不连接后端、CMS、统计或社交 API。

### 5. 响应式和性能

- 以 `944 × 906` 为对照基准，同时检查 `1440 × 900`、`1024 × 768`、`390 × 844`。
- 桌面端追求高保真；移动端允许减少粒子、隐藏自定义光标和简化星图。
- 监听 `visibilitychange`，离屏或页面隐藏时暂停高频动画。
- 不得产生横向滚动；键盘焦点必须可见；Canvas 装饰保持 `aria-hidden`。

## 完成标准

- 六段场景顺序、固定导航、自然滚动、锚点定位和导航激活态完整。
- 页面不依赖参考视频或截图，也不需要联网字体才能显示。
- 现代桌面浏览器中动画流畅，移动端内容完整且表单可操作。
- reduced motion 模式下没有持续位移、鼠标轨迹或密集粒子。
- `pnpm typecheck`、`pnpm lint`、`pnpm test`、`pnpm build` 全部通过。

完成后请汇报：已实现模块、与参考视频仍存在的差异、移动端降级策略、性能处理以及上述四项命令的结果。
